#include <iostream>
#include <string>
#include <ctype.h> //

int main(int argc, char* argv[]) {
  /*== Inizializzazione variabili ==*/
  std::string eq[2];
  int matrice[2][2];

  /*== Input dati ==*/
  std::cout << "Inserisci la prima equazione: ";
  std::getline(std::cin, eq[0]);
  std::cout << "Inserisci la seconda equazione: ";
  std::getline(std::cin, eq[1]);
 // 2x+3y+1 = 0
 // 1/2x+5g+5 = 0
  for (size_t i = 0, x = 0; i < count; i++) {
    switch (eq[x][i]) {
      case '+':
        if
      case '-':
    }
  }
    x++;
  }

}
